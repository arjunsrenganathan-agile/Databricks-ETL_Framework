%yaml
# This is a Databricks asset bundle definition for ETL_Framework.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: ETL_Framework
  uuid: 79b60fd7-1d4a-4c57-8b92-26ea02b5a08b

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

sync:
  include:
    - src/**/*.py
    - src/**/*.ipynb
  
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-117038dd-c0fe.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-117038dd-c0fe.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/arjun.shankar@agile-analytics.com.au to make sure we only have a single copy.
      root_path: /Workspace/Users/arjun.shankar@agile-analytics.com.au/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: arjun.shankar@agile-analytics.com.au
        level: CAN_MANAGE